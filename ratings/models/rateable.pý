from django.db import models

class Rateable(models.Model):
	promotion_type = models.TextField(default=None)
	price = models.DecimalField(max_digits=None, decimal_places=None)

	class Meta:
		app_label = 'promotions'
